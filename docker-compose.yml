services:
  agent:
    build: .
    container_name: agent
    restart: unless-stopped
    env_file: .env
    environment:
      - AGENT_CWD=/workspace
    volumes:
      - ./workspace:/workspace
      - agent-data:/root/.agent
    networks:
      - agent-net

volumes:
  agent-data:

networks:
  agent-net:
